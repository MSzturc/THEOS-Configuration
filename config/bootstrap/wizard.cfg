# Wizard-mode bootstrap: boots on the Linux host MCU so SETUP_WIZARD can run
# and generate the real printer.cfg.
[mcu]
serial: /tmp/klipper_host_mcu

[printer]
kinematics: none
max_velocity: 100
max_accel: 100

[setup_wizard]
config_root: ~/printer_data/config/config
